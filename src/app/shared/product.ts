import { Review } from './review';

export class Product {
    id: string;
    name: string;
    image: string;
    category: string;
    featured: boolean;
    label: string;
    price: string;
    description: string;
    reviews: Review[];
}